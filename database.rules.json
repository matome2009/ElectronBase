{
  "rules": {

    "nonces": {
      "$address": {
        ".read": false,
        ".write": false
      }
    },

    "dev": {
      "master": {
        ".read": "auth != null",
        ".write": false
      },
      "users": {
        "$uid": {
          ".read":  "$uid === auth.uid",
          ".write": "$uid === auth.uid",
          "billing": {
            ".indexOn": ["stripeCustomerId"]
          },
          "points": {
            ".indexOn": ["createdAt", "type"]
          }
        }
      }
    },

    "prd": {
      "master": {
        ".read": "auth != null",
        ".write": false
      },
      "users": {
        "$uid": {
          ".read":  "$uid === auth.uid",
          ".write": "$uid === auth.uid",
          "billing": {
            ".indexOn": ["stripeCustomerId"]
          },
          "points": {
            ".indexOn": ["createdAt", "type"]
          }
        }
      }
    }

  }
}
